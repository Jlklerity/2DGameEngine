#version 410 core


uniform sampler2D texture1;
uniform bool useTexture;

in vec2 TexCoords;
out vec4 FragColor;

void main()
{
    if (useTexture)
        FragColor = texture(texture1, TexCoords);
    else
        FragColor = vec4(1.0, 1.0, 1.0, 1.0); // fallback
     
}
